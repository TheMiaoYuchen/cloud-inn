{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capability; native app commands are the only IPC boundary.",
  "windows": ["main"],
  "permissions": []
}
